; cerf.ini — Configuration for cerf (Windows CE ARM emulator)
;
; Lines starting with ; are comments.
; Boolean values: true/false, yes/no, 1/0

; Device profile to load. Must match a directory name under devices/.
; Contains the virtual filesystem (fs/), registry, and ARM binaries.
device=wince5

; Emulated screen dimensions reported to ARM apps via GetSystemMetrics,
; GetDeviceCaps, and SystemParametersInfoW(SPI_GETWORKAREA).
screen_width=800
screen_height=600

; When true, GetSystemMetrics/GetDeviceCaps return the values above
; instead of the real desktop resolution. Set to false to use native resolution.
fake_screen_resolution=true

; Apply WinCE theme colors from the device registry (HKLM\SYSTEM\GWE\SysColor).
; Colors are applied via IAT hooking — only affects cerf windows, not other apps.
; The 27 WinCE system colors (COLOR_SCROLLBAR through COLOR_STATICTEXT) are loaded
; from the registry on startup. If no registry entry exists, WinCE 5.0 defaults
; are used and written to the registry for future customization.
enable_theming=true

; Strip UxTheme/Aero visual styles from emulated windows to get a classic look
; matching Windows CE. Uses SetWindowTheme per-window — only affects cerf windows.
; Works best combined with enable_theming.
disable_uxtheme=true
